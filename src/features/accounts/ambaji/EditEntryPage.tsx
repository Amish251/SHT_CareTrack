import AccountEditEntry from '../components/AccountEditEntry';
import { ACCOUNTS } from '../config';

export default function AmbajiEditEntryPage() {
  return <AccountEditEntry config={ACCOUNTS.ambaji} />;
}
