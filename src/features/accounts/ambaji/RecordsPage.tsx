import AccountRecords from '../components/AccountRecords';
import { ACCOUNTS } from '../config';

export default function AmbajiRecordsPage() {
  return <AccountRecords config={ACCOUNTS.ambaji} />;
}
