import AccountAddEntry from '../components/AccountAddEntry';
import { ACCOUNTS } from '../config';

export default function AmbajiAddEntryPage() {
  return <AccountAddEntry config={ACCOUNTS.ambaji} />;
}
